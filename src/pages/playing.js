import React, { PureComponent } from 'react'
import Playing from '../components/watch/playing'

export class PlayingPage extends PureComponent {
    render() {
        return (
            <React.Fragment>
                <Playing />
            </React.Fragment>
        )
    }
}

export default PlayingPage
